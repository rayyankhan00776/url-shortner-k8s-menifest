apiVersion: v1
kind: Service
metadata:
  name: node-service
  namespace: us8-namespace
spec:
  selector:
    app: node-app
  ports:
  - protocol: TCP
    port: 4001
    targetPort: 4001
  type: ClusterIP

